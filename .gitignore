.claude/
.env
.env.local
.env.*.local

# built image artifacts (transferred to the VM, never committed)
*.tar.gz
/data/
src/data/

# DB data dumps (migrate-db.sh / manual exports) — never commit
/nanopods-*.sql
/nanopods-*.dump
